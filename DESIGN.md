# 모두의 한국어 디자인 시스템

## 0. 설계 근거

- 기준 화면: 팀이 브라우저에서 승인한 `상황 1 : 리허설·교정 2` 데스크톱 와이어프레임.
- 제품 장면: 표준 만다린 간체자를 주사용 언어로 하는 이주민 학부모가 학원 선생님에게 두 달 휴원과
  재등록 연락 시점을 묻는 대화를 리허설한다.
- 디자인 목표: 사용자가 첫눈에 `오늘의 미션`, `지금 말할 한국어`, `방금 고칠 점`을 순서대로 읽는다.
- 외부 스타일 레퍼런스·이미지·브랜드 모방은 사용하지 않는다. 해커톤 MVP의 실제 역할극 흐름이 시각적 기준이다.

## 1. 분위기와 정체성

차분하지만 행동을 밀어 주는 개인 리허설 공간이다. 정보는 밀도 있게 정리하되 시험지나
채점표처럼 보이지 않아야 한다. 이 제품의 시그니처는 **보라색 미션 블록과 통화 화면의
대비**다. 좌측의 모국어 맥락이 사용자를 안심시키고, 우측의 넓은 한국어 통화 장면이
사용자를 실제 발화로 이끈다.

## 2. 색상

MVP는 라이트 테마만 제공한다. 다크 모드는 60초 데모 범위에 포함하지 않는다.

| 역할 | 토큰 | 값 | 용도 |
| --- | --- | --- | --- |
| 앱 배경 | `--surface-app` | `#FBFCFE` | 전체 배경 |
| 기본 표면 | `--surface-base` | `#FFFFFF` | 통화 패널, 정보 카드 |
| 보조 표면 | `--surface-muted` | `#F7F8FB` | 원문 인용, 비활성 정보 |
| 주 텍스트 | `--text-primary` | `#18212F` | 질문, 핵심 문장 |
| 보조 텍스트 | `--text-secondary` | `#697586` | 레이블, 안내 |
| 구분선 | `--border-default` | `#E4E8EE` | 패널·영역 구분 |
| 주 행동 | `--accent-primary` | `#6046E8` | 말하기 버튼, 진행 표시 |
| 주 행동 강조 | `--accent-strong` | `#4B36CA` | 미션 블록의 깊은 끝색 |
| 한국어 발화 인용 | `--feedback-utterance` | `#EAF4FF` | 사용자의 직전 답변 |
| 언어 교정 | `--feedback-language` | `#FFF3E9` | 어휘·문법 교정 영역 |
| 발음 교정 | `--feedback-pronunciation` | `#F0EDFF` | 발음 전달도 영역 |
| 성공 상태 | `--status-success` | `#1E8A55` | 통화 중, 전달 성공 |
| 경고 텍스트 | `--status-warning` | `#B7531E` | 우선 교정 레이블 |

규칙:

- 보라색은 `말하기`와 미션이라는 행동·방향성에만 쓴다.
- 주황은 오류 자체가 아니라 `먼저 고칠 언어 표현`을 강조할 때만 쓴다.
- 점수의 의미는 색만으로 전달하지 않고 숫자와 상태 문구를 항상 함께 둔다.

## 3. 타이포그래피와 언어

| 수준 | 크기 | 굵기 | 용도 |
| --- | --- | --- | --- |
| 미션 제목 | 30px | 800 | 좌측 미션의 중국어 목표 |
| 상대 질문 | 28px | 800 | 우측에서 가장 먼저 읽는 한국어 질문 |
| 교정 문장 | 18px | 700 | 다시 말할 한국어 문장 |
| 본문 | 14px | 400–700 | 상황·설명·메타데이터 |
| 레이블 | 12px | 700–800 | 상대, 목적, 피드백 축 |
| 보조 안내 | 12px | 400–700 | 중국어 설명, 발음 힌트 |

- 글꼴: `Noto Sans KR`, `Noto Sans SC`, system sans-serif 순으로 사용한다.
- 본문은 14px 아래로 내리지 않는다. 한국어·중국어 줄바꿈은 단어 의미를 깨지 않게 한다.
- **좌측 맥락 패널**은 제목·상황·상대·목적·필수 정보까지 간체 중국어로 쓴다.
- **우측 통화 패널**의 상대 발화와 사용자가 따라 말할 문장은 한국어로 쓴다.
- 교정의 이유, 발음 안내, 도움말은 간체 중국어로 쓴다. 한국어 문장을 중국어로 바꾸지 않는다.

## 4. 여백과 레이아웃

- 기본 단위: 4px.
- 공간 토큰: `4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48 / 64px`.
- 데모 기준 폭: 1280px. 콘텐츠 최대 폭은 1160px, 외곽 여백은 24px이다.
- 데스크톱에서 메인 셸은 `1fr 2fr`이다. 좌측은 상황 브리핑, 우측은 통화·교정 흐름을 맡는다.
- 두 패널의 간격은 16px, 패널 내부 기본 여백은 24px이다.
- 우측 패널은 `통화 헤더 → 상대 질문 → 사용자 답변 → 말하기 행동 → 피드백` 순으로 세로 흐름을 유지한다.
- 피드백 내부는 언어 교정 1.15 : 발음 교정 0.85의 비율을 쓴다. 목표 문장이 점수보다 먼저 읽혀야 한다.
- 해커톤 MVP는 데스크톱 시연이 기준이며, 모바일 반응형은 구현 범위에서 제외한다.

## 5. 컴포넌트

### 리허설 셸

- **구조**: 상단 제품명·진행 표시 + 좌측 맥락 패널 + 우측 통화 패널.
- **상태**: 상황 확인 중, 통화 중, 즉시 피드백, 재시도, 짧은 마무리.
- **접근성**: 화면 제목과 현재 단계가 읽혀야 하며, 패널 구분에만 색을 의존하지 않는다.

### 미션 블록

- **구조**: `오늘의 미션` 레이블, 간체 중국어 제목, 중국어 한 줄 설명.
- **표면**: `--accent-primary`에서 `--accent-strong`으로만 이어지는 보라색 깊이.
- **역할**: 좌측 상단의 가장 강한 시각 포인트. 장식 문구를 추가하지 않는다.

### 상황 브리프

- **구조**: 중국어 원문 인용 + `对方 / 目的 / 方式` 3행 + `需要说清楚` 목록.
- **상태**: 기본, AI가 추가 확인을 요청한 상태, 정보가 아직 없는 상태.
- **규칙**: 사용자가 말하지 않은 정보는 오류로 표시하지 않는다. AI의 다음 질문으로 받는다.

### 통화 헤더와 상대 질문

- **구조**: 상대 식별, 속도·목적 메타데이터, 통화 상태, 큰 한국어 질문.
- **상태**: 질문 대기, AI 발화 중, 사용자 녹음 중, 다음 질문 준비.
- **규칙**: 상대 질문이 우측 패널에서 가장 큰 글자다. 실제 상대 이름과 색상만으로 역할을 구분하지 않는다.

### 3-turn 역할극 범위

- **생성 단위**: 장면을 만들 때 `max_turns: 3`과 세 turn의 목적을 함께 확정한다. 첫 demo의 목적은
  `휴원 시작·기간 확인 → 퇴원 아닌 일시 휴원 확인 → 재등록 연락 시점 문의` 순서다.
- **질문 생성**: 세 질문 문장을 처음부터 고정하지 않는다. 각 turn의 목적만 script에 잠그고, 실제 질문은
  직전 답변과 이미 전달된 필수 정보를 반영해 만든다. 사용자가 앞 turn에서 뒤의 정보까지 말했다면 같은
  질문을 반복하지 않고 남은 목적이나 확인 질문으로 넘어간다.
- **진행 표시**: 통화 header에 `对话 1/3`, `对话 2/3`, `对话 3/3`을 표시한다. 사용자가 현재 범위와
  남은 대화를 알 수 있어야 한다.
- **turn loop**: 각 turn은 `상대 질문 → 한국어 답변 → 어휘·문법/발음 feedback → 같은 문장 retry 또는
  다음 질문`으로 진행한다. retry는 turn당 최대 한 번이며 turn 수에 포함하지 않는다.
- **종료**: 3번째 turn을 끝내거나 세 목적이 모두 전달되면 마무리 카드로 이동한다. 한 turn 뒤에 바로
  종료하지 않으며, 정보가 부족해도 정해진 세 turn을 초과하지 않는다.
- **60초 demo**: 첫 turn에서 feedback과 retry를 모두 보여 주고, 2·3번째 turn은 짧은 답변과 확인 상태로
  진행한다. 자유 대화처럼 무한히 이어지는 구조는 구현하지 않는다.

### 말하기 행동

- **구조**: 큰 원형 `말하기` 버튼 + 다음 행동을 설명하는 짧은 문장.
- **상태**: 준비, 녹음 중, 분석 중, 재시도, 분석 실패.
- **접근성**: 키보드로 실행 가능하고, 녹음 중·분석 중 상태가 텍스트로 갱신된다.
- **오류**: 음성 전사 실패 시 점수나 오류를 추측하지 않고 간체 중국어로 재녹음을 안내한다.

### 어휘·문법 교정 패널

- **구조**: 우선순위 레이블, 완성된 추천 한국어 문장, 간체 중국어 설명.
- **상태**: 의미 오류, 문법·존댓말, 자연스러움, 교정 없음.
- **규칙**: 개수 제한은 두지 않지만 의미 오류 → 문법·존댓말 → 자연스러움 순으로 정렬한다. 사용자의 재시도 목표는 여러 피드백을 반영한 하나의 완성 문장이다.

### 발음 전달도 패널

- **구조**: `발음 전달도`, 숫자 점수, 상태 문구, 진행 막대, 집중 단어와 중국어 안내.
- **점수 상태**: 85–100 매우 또렷함 / 70–84 또렷함 / 50–69 전달 가능 / 0–49 다시 연습 권장.
- **규칙**: 점수는 문장 단위의 전달도이며 전체 한국어 실력·합격 여부가 아니다. 다음 질문으로 이동을 막지 않는다.
- **재시도**: 같은 목표 문장으로 다시 녹음하면 새 점수와 이전 점수를 비교한다. 다른 표현으로 바꾸면 새 목표 문장 기준으로 시작한다.

### 발화 변화 그래프

- **목적**: 사용자가 첫 발화와 같은 문장의 재시도를 직접 비교하는 사람의 검토 근거다. 발음 점수의
  산식이나 정답 판정에는 사용하지 않는다.
- **구조**: 재시도 결과에서 `第一次`와 `再试一次` 두 pitch contour를 하나의 graph에 겹쳐 표시한다.
  X축은 각 발화의 시작부터 끝까지 0–100%로 정규화한 시간이고, Y축은 각 녹음의 중앙 pitch를
  기준으로 바꾼 speaker-relative semitone이다.
- **추출**: browser에서 실제 녹음을 decode하고 75–500Hz 구간의 voiced frame만 YIN으로 추출한다.
  원본 audio와 추출 결과는 장기 저장하지 않는다.
- **색상**: 첫 발화는 `--accent-primary`, 재시도는 `--status-success`를 쓴다. 색과 함께 line pattern,
  legend, `첫 발화 / 재시도` text를 제공한다.
- **해석 경계**: 검증된 Korean reference audio가 없으므로 `정답 곡선`, `정확한 억양`, `원어민과
  일치`라고 표시하지 않는다. 곡선이 비슷하거나 달라졌다는 사실만으로 발음 향상을 단정하지 않는다.
- **근거 부족**: voiced frame이 충분하지 않거나 decode에 실패하면 graph를 만들지 않고
  `声音太短，无法比较音高变化。请再录一次。`를 표시한다.
- **접근성**: graph 아래에 첫 발화·재시도의 유효 구간 수와 pitch 범위를 text로 요약한다. canvas만으로
  의미를 전달하지 않는다.

## 6. 움직임과 상호작용

- 말하기 버튼의 누름·해제, 피드백 표시, 점수 갱신, 발화 변화 graph 갱신만 움직임을 쓴다.
- 마이크 입력 뒤에는 `녹음 중 → 분석 중 → 결과`의 현재 상태를 텍스트로 보여 준다.
- 피드백은 위에서 아래로 200ms 이내에 나타난다. 점수 막대는 새 점수로만 전환한다.
- `prefers-reduced-motion`에서는 이동·확장 애니메이션을 제거하고 상태만 즉시 바꾼다.

## 7. 깊이와 표면

혼합 전략을 쓴다.

- 통화 패널과 브리프는 얇은 `--border-default`와 흰 표면으로 나눈다.
- 미션 블록과 말하기 버튼만 부드러운 그림자를 사용해 행동 우선순위를 만든다.
- 언어·발음 피드백은 각자의 옅은 배경색으로 분리하고, 추가 카드 테두리·그림자를 중첩하지 않는다.

## 8. 접근성 제약과 수용한 부채

### 제약

- WCAG 2.2 AA 대비를 목표로 한다.
- 모든 행동은 마우스와 키보드로 접근 가능해야 한다.
- 숫자 점수에는 상태 문구를 함께 제공하고, 색만으로 성공·연습 필요를 구분하지 않는다.
- 발화 변화 graph에는 line pattern·legend와 text 요약을 함께 제공한다.
- 간체 중국어·한국어 혼합 화면에서 언어별 `lang` 속성을 지정한다.
- 발음 점수는 사용자의 능력 등급이나 합격 표시로 사용하지 않는다.

### 수용한 부채

| 항목 | 위치 | 이유 | 해소 조건 |
| --- | --- | --- | --- |
| 모바일 레이아웃 미구현 | 리허설 셸 | 60초 데스크톱 시연이 현재 스코프 | 데모 이후 실제 사용자 테스트 전 375px부터 설계 |
| 장기 학습 이력·점수 추이 없음 | 발음 전달도 | 한 장면 리허설 MVP에만 집중 | 후속 세션 설계 시 사용자 동의와 함께 추가 |
