# AGENTS.md — Codex 해커톤 (2026-09-04)

2인 페어 · 2시간 30분 · 목표는 **입상**. 짧게 유지한다.
응답 스타일·검증 규율은 `~/.codex/AGENTS.md` 에 있고 여기서 반복하지 않는다.

## 제품 (⚠️ 미확정 — 확정되면 이 절만 갈아끼운다)

**안 본 쟁점 리포트.** 이슈 하나 + 사용자가 본 기사·영상 URL 몇 개를 받아,
그 이슈에 실재하는 쟁점 축 전체를 뉴스에서 뽑아 분해하고, 사용자가 본 것과 **차집합**을 낸다.

출력: "이 이슈엔 축이 N개인데 당신은 M개만 봤다. 나머지는 이것이고 근거 기사는 여기다."

**누가 옳은지는 판정하지 않는다.** 축을 세고 뺄 뿐이다. 이 선을 넘는 기능 제안은 거절할 것.

## 스코프 규율 — 여기가 제일 중요

**60초 데모 대본이 스펙이다.** 대본에 나오지 않는 것은 만들지 않는다.

- 기능이 떠오르면 먼저 물어라: "이게 대본의 어느 초에 나오나?" 안 나오면 안 만든다.
- 로그인·회원가입·설정 화면·반응형·다크모드·에러 페이지 — 전부 안 만든다.
- 예쁘게 만드는 것보다 **끝까지 도는 것**이 먼저다. end-to-end 를 먼저 세우고 살을 붙인다.

## 리듬

30분마다 **동작하는 상태로** 합친다. 30분 넘게 안 도는 상태로 가고 있으면 그 자체가 신호다 —
기능을 줄여서 도는 상태로 되돌린다.

## 이 레포에 넣지 않는 것

개인정보(연락처·여권명·학적·성적). 팀원과 공유되는 레포다.
